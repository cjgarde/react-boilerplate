import React, { Component } from 'react';
import { Trans } from '@lingui/macro';

class Layout extends Component {
  render() {
    return (
      <div>
        <Trans>This is the layout</Trans>
      </div>
    );
  }
}

export default Layout;
